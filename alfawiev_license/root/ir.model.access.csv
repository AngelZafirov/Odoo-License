# license.py
from odoo import models, fields, api

class SoftwareLicense(models.Model):
    _name = 'license.license'
    _description = 'Software License'

    name = fields.Char(...)  # Lizenzschlüssel wie gehabt
    product_id = fields.Many2one('license.product', required=True)
    customer_id = fields.Many2one('res.partner', required=True)

    def name_get(self):
        res = []
        for rec in self:
            label = f"{rec.product_id.name} - {rec.customer_id.name} - {rec.name[:8]}"
            res.append((rec.id, label))
        return res
